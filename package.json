{
  "name": "cleanspot",
  "version": "1.0.0",
  "private": true,
  "description": "CleanSpot hackathon MVP - Report. Clean. Reward. Reuse.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server/server.js",
    "start": "node server/server.js",
    "check": "node --check server/server.js && node --check server/services/ai.js && node --check public/js/app.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "zod": "^4.1.12"
  }
}
